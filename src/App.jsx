import { BrowserRouter, Routes } from "react-router-dom";
// import { routePaths } from 

export const App = () => {
  <BrowserRouter>
    <Routes>
      {routePaths()}
    </Routes>
  
  </BrowserRouter>
};
