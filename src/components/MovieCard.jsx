import { Link } from "react-router-dom"

export const MovieCard = () => {
  return (
    <Link>
      <article>
        <img src={ } alt={title}
        />
        <div>
          <p>Released {release_date}</p>
        </div>
      </article>
    </Link>
  )
}